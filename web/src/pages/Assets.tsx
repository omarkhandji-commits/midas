import { PagePlaceholder } from "@/components/PagePlaceholder";

export function AssetsPage() {
  return (
    <PagePlaceholder
      kicker="Asset Studio"
      title="Offers, landing copy, outreach, briefs, proposals"
      description="Generate, edit, copy and download the business assets MIDAS prepares for a move. Outputs are drafts only — placeholders like {{first_name}} are preserved on purpose."
    />
  );
}
