import { PagePlaceholder } from "@/components/PagePlaceholder";

export function ApprovalsPage() {
  return (
    <PagePlaceholder
      kicker="Approval Center"
      title="Nothing leaves the machine until approved"
      description="Every outbound or irreversible action queues here for your decision. Resolutions are written to the receipts ledger and dispatched through the same queue every channel shares."
    />
  );
}
