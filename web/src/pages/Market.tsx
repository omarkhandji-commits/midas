import { PagePlaceholder } from "@/components/PagePlaceholder";

export function MarketPage() {
  return (
    <PagePlaceholder
      kicker="Market Radar"
      title="Track competitors with dated, sourced snapshots"
      description="Add a competitor URL, MIDAS watches and writes diffs into MARKET memory. No private scraping, no ads API — only what is publicly fetchable."
    />
  );
}
