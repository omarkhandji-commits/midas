import { PagePlaceholder } from "@/components/PagePlaceholder";

export function OutcomesPage() {
  return (
    <PagePlaceholder
      kicker="Outcome Tracker"
      title="Record what happened so future runs improve"
      description="Replies, sales, clicks, misses, errors. Outcomes are stored as RESULT memory and written as receipts — the same proof model as every other event."
    />
  );
}
