import { PagePlaceholder } from "@/components/PagePlaceholder";

export function MissionsPage() {
  return (
    <PagePlaceholder
      kicker="Missions"
      title="Run a scan, get a Daily Revenue Move"
      description="Give MIDAS a niche. It researches with cited sources, scores opportunities, and prepares the prepared move and assets. It stops before any outbound action."
    />
  );
}
