import { PagePlaceholder } from "@/components/PagePlaceholder";

export function MemoryPage() {
  return (
    <PagePlaceholder
      kicker="Business Memory"
      title="User, business, decision, result, market, error"
      description="Six durable namespaces. Append-only with supersede so history is preserved. Sourced facts are graded MEDIUM or HIGH; unsourced facts stay LOW."
    />
  );
}
