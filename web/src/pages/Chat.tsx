import { PagePlaceholder } from "@/components/PagePlaceholder";

export function ChatPage() {
  return (
    <PagePlaceholder
      kicker="Daily Revenue Move"
      title="Proof first. Draft second. Approval before action."
      description="Chat with MIDAS using your own LLM key. Risky actions are not executed — they appear as an approval card you tap. Streaming chat with proof citations and a live cost meter lands in the next sprint."
    />
  );
}
