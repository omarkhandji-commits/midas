import { PagePlaceholder } from "@/components/PagePlaceholder";

export function ChannelsPage() {
  return (
    <PagePlaceholder
      kicker="Channel Hub"
      title="Approve from your phone, your team chat, your inbox"
      description="Telegram, Discord, Slack, WhatsApp, Email, SMS, Signal, iMessage. Connect each by pasting a token — MIDAS delivers drafts and receives approvals. It never auto-sends."
    />
  );
}
