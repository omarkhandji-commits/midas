# SEO Report

## Recommended Angle

Primary angle: self-hosted AI agent for safe LLM workflows.

Secondary angle: local-first dashboard and CLI for approval-gated automation with
signed receipts, budget controls, Ollama, and MCP.

This angle matches the codebase: MIDAS has a Python CLI, FastAPI dashboard,
approval queue, receipt ledger, budget controls, Ollama/cloud provider setup,
MCP-related tooling, and a local-first install path.

## GitHub Description

Self-hosted AI agent for safe LLM workflows: approvals, receipts, budget controls, local dashboard, CLI, Ollama, and MCP.

## Recommended Topics

- `ai-agent`: broad discovery topic for agent projects.
- `llm-agent`: direct match for LLM workflow users.
- `autonomous-agent`: common search term; MIDAS constrains autonomy with approvals.
- `self-hosted`: matches local dashboard and user-controlled setup.
- `local-first`: central product principle.
- `ollama`: supported local model path.
- `mcp`: relevant for tool workflows and discovery.
- `python`: main backend language.
- `fastapi`: dashboard backend.
- `react`: dashboard frontend.
- `cli`: core user surface.
- `dashboard`: core user surface.
- `approval-workflow`: strong differentiator.
- `audit-trail`: matches receipts and proof export.
- `ed25519`: receipt signing detail.
- `agent-safety`: safety-focused discovery.
- `prompt-injection`: security test and threat-model topic.
- `llm-security`: security audience topic.
- `remotion`: supported media workflow.
- `open-source`: repository discovery topic.

## Keywords

Primary keywords:

- self-hosted AI agent
- local-first AI agent
- LLM agent
- approval workflow
- signed receipts
- budget controls
- local dashboard
- CLI agent
- Ollama agent
- MCP tools

Secondary keywords:

- audit trail
- Ed25519 receipts
- prompt injection defense
- LLM security
- FastAPI dashboard
- React dashboard
- Remotion video workflow
- open-source agent

Keywords to avoid:

- best AI agent
- guaranteed results
- fully autonomous money agent
- secure by default
- compliant
- certified
- one-click passive income
- replaces developers

## README Changes

- Updated first impression around self-hosted, local-first LLM workflows.
- Replaced the static CI badge with real GitHub Actions badge URLs.
- Added CodeQL badge.
- Added a visible disclaimer link.
- Added `Who It Is For`.
- Added `Use Cases`.
- Replaced the placeholder clone URL with the real repo URL.
- Kept the README structure and existing technical content.

## Search Queries MIDAS Should Match

- self hosted ai agent with approvals
- local first llm agent dashboard
- ollama agent with dashboard
- llm workflow approval queue
- ai agent signed receipts
- mcp approval workflow
- open source llm agent cli
- fastapi react ai agent dashboard
- prompt injection safe agent workflow

## Social Preview

Recommended format: `1280x640`.

Recommended text:

```text
MIDAS
Local-first AI agent
Approvals. Receipts. Dashboard. CLI.
```

Use the supplied MIDAS logo on a dark background.

## Launch Messages

Show HN:

```text
Show HN: MIDAS, a local-first AI agent with approvals and signed receipts

I built MIDAS for people who want to try LLM automation without giving an agent
silent access to files, tools, or external services. It runs from a local
dashboard or CLI, supports Ollama and cloud providers, and queues risky actions
for review before they execute.
```

LinkedIn:

```text
I published MIDAS, an open-source local-first AI agent for LLM workflows that
need approvals, signed receipts, budget controls, a dashboard, CLI, Ollama, and
MCP support.

The goal is simple: make agent actions reviewable before they touch files,
services, or external tools.
```

Reddit/dev community:

```text
I released MIDAS, a self-hosted AI agent focused on approval-gated LLM
workflows. It has a local dashboard, CLI, Ollama support, MCP support, budget
controls, signed receipts, and an independent receipt verifier.

I would value feedback on the install flow, approval model, and receipt format.
```

## GitHub CLI

GitHub CLI was not installed during preparation. After installing and
authenticating `gh`, run the command in [GITHUB_SETUP.md](GITHUB_SETUP.md).

## Manual GitHub Tasks

- Confirm the repository exists as `omarkhandji-commits/midas`.
- Add the description and topics in the GitHub About box.
- Upload a `1280x640` social preview.
- Confirm GitHub Actions badges render after the first push.
- Create the first release only after a separate approval.

## Remaining Risk

GitHub topic and badge rendering can only be verified after the repository exists
and the first push completes.
